import { HorizontalSpacing } from "../_Spacing/HorizontalSpacing";

export default async function Header() {
    return <header className="bg-green-400">
        <HorizontalSpacing>
            Header
        </HorizontalSpacing>
    </header>
}